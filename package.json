{
  "name": "trailapp",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../backend && npm ci && npx prisma generate",
    "start": "cd backend && npx prisma migrate deploy && node src/server.js"
  }
}
