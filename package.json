{
  "name": "trailapp",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../backend && npm ci && npx prisma generate && npx prisma migrate deploy",
    "start": "cd backend && node src/server.js"
  }
}
